//your JS code here. If required.
// Take first input and convert to number
let firstInput = Number(prompt());

// Multiply first input by 2
let result = firstInput * 2;

// Take second input and convert to number
let secondInput = Number(prompt());

// Add second input to result
result = result + secondInput;

// Display final result
alert("The final result is: " + result);
